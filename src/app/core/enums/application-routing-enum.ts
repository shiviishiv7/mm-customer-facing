export enum ApplicationRoutingEnum {
  DASHBOARD = '/dashboard',
  LANDING_PAGE = '/landing',
  INSTANCE_MEETING = 'instance-meeting',  // matches live URL /dashboard/instance-meeting
  SCHEDULED_MATCH = 'scheduled-match',
}
