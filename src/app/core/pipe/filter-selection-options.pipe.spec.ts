import { FilterSelectionOptionsPipe } from './filter-selection-options.pipe';

describe('FilterSelectionOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectionOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
